import React from "react";

/* Shows available spot(s) and allows user to register for an appointment in an avaiable spot*/
export default function Empty(props) {
  return (
    <main className="appointment__add">
      <img
        className="appointment__add-button"
        src="images/add.png"
        alt="Add"
        onClick={props.onAdd}
      />
    </main>
  );
}
